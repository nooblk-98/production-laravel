services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: production-laravel:latest
    container_name: production-laravel
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - laravel-storage:/var/www/html/storage
    restart: unless-stopped

volumes:
  laravel-storage: